/**
 * Fixed locale for all date/time formatting — deliberately not
 * auto-detected from the browser/OS. This controls dates in the exported
 * DOCX agenda, an official document that should look identical regardless
 * of which admin's machine generates it.
 */
export const APP_LOCALE = 'en-GB';
