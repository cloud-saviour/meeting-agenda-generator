rules_version = '2';

// Local emulator only — no real Firebase project exists yet.
// Open rules are fine here since nothing is ever deployed with this project id.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
